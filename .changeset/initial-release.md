---
"tiptap-extension-ruby": minor
---

Initial public release of `tiptap-extension-ruby`: a TipTap v3 extension that renders HTML `<ruby>` (furigana) for CJK text. Ships the `Ruby` atom node, `setRuby` / `toggleRuby` / `unsetRuby` commands, and an Aozora-Bunko-style `|漢字《かんじ》` input/paste rule.
