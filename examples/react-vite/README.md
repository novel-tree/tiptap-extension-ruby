# React + Vite Example

This sandbox demonstrates the intended product UX for the Ruby extension:

- select text inside the editor
- open a modal to enter the reading
- apply ruby without relying on hard-to-type punctuation

It also includes locale presets that swap shorthand delimiters for Japanese, Chinese, and Korean-oriented flows.

## Run

```bash
pnpm install
pnpm dev
```
